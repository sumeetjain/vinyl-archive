<%= form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :the_photo %><br>
    <%= f.file_field :the_photo %>
  </div>
  <div class="field">
    <%= f.label :album_id %><br>
    <%= f.collection_select :album_id, Album.where("id = ?", params['format']), :id, :name, :prompt => 'Select An Album' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
